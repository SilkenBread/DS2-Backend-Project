name: Deployment

on:
  pull_request:
    types: [closed]
    branches: [ "main" ]

jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
          - name: Deploy to production
            env:
                deploy_url: ${{ secrets.RENDER_DEPLOY_HOOK_URL }}
            run: |
              curl "$deploy_url"
